services:
  - type: web
    name: text-to-pptx
    env: docker
    plan: free
    autoDeploy: true
    dockerCommand: uvicorn backend.main:app --host 0.0.0.0 --port 8000
    healthCheckPath: /healthz
